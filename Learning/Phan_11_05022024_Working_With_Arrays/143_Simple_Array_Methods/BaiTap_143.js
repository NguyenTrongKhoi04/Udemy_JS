'use strict';

let arr =['a','b','c','d','e'];
const arr2 = ['j', 'i', 'h', 'g', 'f'];

/**
 * * Sử dụng những dữ liệu cho sẵn
 * TODO 1) Dự đoán output 
 *        ? console.log(arr.slice(2));
 *        ? console.log(arr.slice(2, 4));
 *        ? console.log(arr.slice(-2));
 *        ? console.log(arr.slice(-1));
 *        ? console.log(arr.slice(1, -2));
 *        ? console.log(arr.slice());
 * TODO 2) Sự khác biệt giữa "console.log(arr.slice());" & "console.log([...arr]);"
 * TODO 3) 'slice' chỉ giúp ta tạo ra bản sao, ko tác động vào arr gốc =>> lm sao để tác động đc vào arr gốc 
 *            ? hoạt động của hàm splice
 *            ? sự khác biệt hàm splice so với slice
 * TODO 4) Đảo ngược vị trí mảng ban đầu (tác động vào mảng gốc )
 * TODO 5) Nối 2 mảng arr & arr2 thành 1 mảng mới
 *          ? làm bằng destructor
 *          ? Làm bằng cách khác
 * TODO 6) Nối các phần tử của mảng mới phần 5) thành 1 chuỗi str
 * TODO 7) REMIND: pop, push, shift, unshift, indexOf, lastIndexOf, split, join, sort
 *          ? Tác dụng của  các method trên
 *          ? return trả về gì (nếu method có)
 */
