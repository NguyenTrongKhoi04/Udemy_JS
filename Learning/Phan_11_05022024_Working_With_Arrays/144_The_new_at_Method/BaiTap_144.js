'use strict';

const arr = [23,11,64];

/**
 * TODO 1) Lấy giá trị cuối cùng bằng cách (giả sử ta không biết dộ dài arr) 
 *         ? Sử dụng index arr
 *         ? slice 
 * TODO 2) Sử dụng 'at()' để làm 1) 
 *          ? cách hoạt động của 'at'
 *          ? khi nào ta nên dùng 'at'
 */ 