"use strict";

let height;

/**
 * * Sử dụng dữ liệu được cung cấp ở trên
 * TODO 1) Kể tên 5 giá trị falsy
 * TODO 2) Dự đoán out put
 *          ? console.log(Boolean(0));
 *          ? console.log(Boolean(undefined));
 *          ? console.log(Boolean("Jonas"));
 *          ? console.log(Boolean({}));
 *          ? console.log(Boolean(""));
 * TODO 3) Dự đoán ouput console.log(height);
 */
