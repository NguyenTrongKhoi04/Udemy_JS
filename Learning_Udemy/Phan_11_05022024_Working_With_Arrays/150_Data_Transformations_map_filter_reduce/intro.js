/**
 * * lưu ý cả 3 đối số trong func sẽ giống với đối số func trong forEach
 *       * áp dụng với filter, reduce
 *       ? 3 Đối số ở đây là value - index - arr =>> nếu muốn bỏ qua đối số ở giữa thì dùng "_"
 *       ! chỉ riêng reduce là ngoài có 3 đối số kia thì có thêm 2 tham số (lưu trữ, giá trị bắt đầu)
 */