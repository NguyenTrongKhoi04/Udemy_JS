'use strict';

const movements = [200, 450, -400, 3000, -650, -130, 70, 1300]; 

/**
 * * Sử dụng những dữ liệu cho sẵn
 * TODO 1) "movements" là nạp tiền và rút tiền (tương ứng với +/-)
 *          ! Sử dụng for of
 *          ? Nếu là (+) =>> output: ` user nạp ${só tiền}`
 *          ? Nếu là (-) =>> output: ` user rút ${só tiền}`
 * TODO 2) Sử dụng "forEach()" để làm 1) 
 * TODO 3) Tiếp tục với for of phần 1). Hãy in ra dưới dạng `Movement ${index phần tử}: User (rút/nạp) ${number} tiền`
 * TODO 4) Dùng forEach làm phần 3). Điểm yếu lớn nhất của "forEach"
 */