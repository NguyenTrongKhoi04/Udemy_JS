'use strict';

/**
 * * Sử dụng những dữ liệu trên 
 * TODO 1) Cho 3 log sau "console.log(document.documentElement);console.log(document.head);console.log(document.body);"
 *        ? Theo bạn liệu output của chúng có bao gồm các phần tử con trong chúng hay k 
 * TODO 2) cho 2 đoạn code sau
 *          "const allButtons = document.getElementsByTagName('button');console.log(allButtons);" 
 *          "const allSections= document.querySelectorAll('.section');console.log(allSections);"
 *          ? Bạn hãy thử F12 và xóa 1 phần tử ở html trong mỗi biến trên. Sau đó gọi lại biến đó bằng js ở DevTools
 *          ? Từ trên hãy rút ra kết luận 
 * TODO 3) "console.log(document.getElementsByClassName);" =>> output? danh sách đó có tự động cập nhật không?
 * TODO 4) Dùng js tạo 1 khối sau và cho chúng hiển thị lên màn hình (ở bất kỳ đâu cũng đc)
 *        <div class="cookie-message">We use cookied for improved functionality and analytics. <button class="btn btn--close-cookie">Got it!</button></div>
 * TODO 5) Cho khối trên vào 
 *          ? đầu danh sách thẻ con của khối .header
 *          ? cuối danh sách thẻ con của khối .header
 *          ! Tìm hiểu và nghiên cứu cloneNode()
 * TODO 6) Chèn khối ở phần 5) 
 *          ? đứng trước khối .header
 *          ? đứng sau khối .header
 * TODO 7) Tạo sự kiện khi nhấn vào button ở phần 5) thì sẽ xóa toàn bộ khối ở phần 5) đi 
 */