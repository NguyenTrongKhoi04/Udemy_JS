'use strict';

/**
 * TODO 1) Hãy thử tắt chế dộ nghiêm ngặt ("use strict") và chạy các lệnh sau
 *          ? test = true;
 *          ? const interface = 'Audio';
 *          ? const private = 534;
 *          ? if = 5
 */ 
