"use strict";

/**
 * TODO 1) Hãy thử tắt chế dộ nghiêm ngặt ("use strict") và chạy các lệnh sau
 *          ? test = true;
 *          ? const interface = 'Audio';
 *          ? const private = 534;
 *          ? if = 5
 */

// TODO 1)
test = true; // chưa khai báo phạm vi biến
const interface = "Audio"; // interface dịch là giao diện =>> gán vào âm thanh =>> vô lý
const private = 534; // private nhưng lại để 1 số công khai
