
/**
 * * TIPS 1: bị bug =>> dùng console.log(); nhưng cũng đừng quên "console" có rất nhiều họ hàng của nó (console.table, console.warn,...)
 * * TIPS 2: Bạn muốn xem luồng kết quả chạy của dự án =>> F12 > mở source để thấy code và ouput của dùng code đó > phía bên phải có nút chạy lại web 
 *                                                              ! nhấn nút đó để chạy lại web từ đó sẽ thấy được output của từng dòng code
 */